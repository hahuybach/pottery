import ProductDetail from '../../layouts/Main/ProductDetail';

export default function TeasetDetail(props: any) {
    return (
        <>
            <ProductDetail props={props} />
        </>
    );
}
